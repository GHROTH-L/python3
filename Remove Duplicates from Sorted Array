class Solution:
    def removeDuplicates(self, nums: List[int]) -> int:
        """
        :type nums :list[int]
        :rtype :int
        要將重複的數字刪除 output有出現的
        """
        r = []
        for i in nums:
            if i not in r:
                r.append(i)
        nums[:] = r
        
      '''
      缺點：花太久時間
      '''
